//
// Content module
//

import { Localised } from './utils'

/** Markdown content in multiple languages that has been processed into HTML */
export interface LocalisedContent {
  content: Localised
}
